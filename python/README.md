## Usage of python codes

Most python code in this directory rely on the C++ library and examples scripts being built. The python files provide bindings for easier simulation parameter settings, and easier result analysis. If the scripts are not working, make sure to point to your built programs where necessary.